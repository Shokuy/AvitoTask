services:
  server:
    build: ./server
    ports:
      - "8080:8080"

  client:
    build:
      context: .
      args:
        VITE_API_BASE: ""
    ports:
      - "3000:80"
    depends_on:
      - server
